{
  "name": "items-api",
  "private": true,
  "version": "1.0.0",
  "main": "build/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/src/index.js --inspect",
    "build": "npm run --silent barrels && tsc && npm run --silent generate-spec && echo 'Build complete'",
    "barrels": "barrelsby --directory 'src/controllers' --location top --name barrel --delete",
    "generate-spec": "swaggerGen -c ./swaggerConfig.json"
  },
  "dependencies": {
    "@common/backend": "*"
  },
  "devDependencies": {
    "barrelsby": "^2.1.1",
    "typescript": "^3.5.2",
    "typescript-rest-swagger": "^0.1.0"
  }
}

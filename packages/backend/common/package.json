{
  "name": "@griffins/backend",
  "private": true,
  "version": "1.0.0",
  "main": "build/src/index.js",
  "scripts": {
    "build": "npx tsc && echo 'Build complete'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@griffins/logging": "*",
    "express": "^4.17.1",
    "typescript-rest": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^3.5.2"
  }
}

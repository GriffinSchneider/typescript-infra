{
  "name": "@common/backend",
  "private": true,
  "version": "1.0.0",
  "main": "build/src/index.js",
  "scripts": {
    "build": "npx tsc",
    "watch": "nodemon -q -x 'npm run --silent build && echo \"Build complete\"' --ext ts --ignore build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "typescript-rest": "^2.1.0"
  }
}
